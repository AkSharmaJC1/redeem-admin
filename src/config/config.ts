const config = {
	baseUrl: import.meta.env.VITE_BASE_URL,
	RememberMeKey: import.meta.env.VITE_REMEMBERED_KEY,
	EncryptTransformKey: import.meta.env.VITE_ENCRYPT_TRANSFORM_KEY,
};

export default config;
